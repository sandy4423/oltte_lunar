/**
 * 매장 식별자 (store_id)
 *
 * 올때만두(1호점)와 올때빙수(2호점)가 같은 Supabase DB 를 함께 씁니다.
 * `orders.store_id` 로 두 매장의 주문이 구분되며, 장부앱도 같은 값을 씁니다
 * (`sale_campaigns.store_id`). 그래서 이 앱의 관리화면도 같은 기준으로 가릅니다.
 *
 * 중요 — `orders.store_id` 는 DB 에서 NOT NULL 이고 DEFAULT 가 올때만두입니다.
 * 이 앱은 주문을 넣을 때 store_id 를 지정하지 않으므로, 이 앱에서 생긴 새 주문은
 * 자동으로 올때만두가 됩니다. (2026-09-22 DB 실측 확인)
 *
 * 캠페인 slug('tteokguk26' 등)로 가르지 않는 이유:
 * 떡국만두 캠페인은 만두 매장 것이라 이 화면에 보여야 하고, 과일 캠페인은 빙수
 * 매장 것이라 빠져야 합니다. 캠페인은 계속 새로 생기므로 slug 를 코드에 박으면
 * 새 캠페인이 생길 때마다 코드를 고쳐야 합니다. 매장으로 가르면 그럴 일이 없습니다.
 */

/** 올때만두 (1호점) */
export const MANDU_STORE_ID = '75ff8c52-83b2-4bb8-85e4-e1806d6e4646';

/** 올때빙수 (2호점) */
export const BINGSU_STORE_ID = 'a7208ad0-b916-4c83-ae88-d537af9b3c47';
